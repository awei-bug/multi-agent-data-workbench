# Defense In Depth

找到根因之后，如果问题容易再次通过别的入口出现，可以在多个关键边界补轻量防护。

## 适用场景

- 输入数据结构不稳定
- 前后端约定容易漂移
- 异步状态容易进入非法组合
- 用户操作顺序可能绕过单点校验

## 做法

- 在入口做基础校验
- 在关键状态转换处做断言或保护
- 在边界层保留可诊断信息
- 但不要用重复、互相矛盾的补丁掩盖根因

## 目标

先修根因，再决定是否需要分层防护；不要把“加更多判断”当成定位问题的替代品。
